variables:
  - name: provider
    value: AzureAD

trigger:
  - master

pool:
  vmImage: ubuntu-latest

steps:
  - task: Bash@3
    displayName: Set provider version variable
    env:
      BUILD_NUMBER: $(Rev:r)
    inputs:
      targetType: inline
      script: echo "##vso[task.setvariable variable=providerVersion]$(dotnet list package | grep -oP 'AzureAD[ ]*\K[1-9][0-9.]*')$(BUILD_NUMBER)"
    
  - task: UseDotNet@2
    displayName: Use .NET 5 SDK
    inputs:
      packageType: sdk
      version: 5.x
      installationPath: $(Agent.ToolsDirectory)/dotnet
    
  - task: DotNetCoreCLI@2
    displayName: Build
    inputs:
      command: build
      projects: '**/Pulumi.FSharp.$(provider).fsproj'

  - task: DotNetCoreCLI@2
    displayName: Pack
    inputs:
      command: pack
      packagesToPack: '**/Pulumi.FSharp.$(provider).fsproj'
      versionEnvVar: providerVersion

  - task: PublishBuildArtifacts@1
    displayName: Push
    inputs:
      pathToPublish: $(Build.ArtifactStagingDirectory)/Pulumi.FSharp.$(provider).$(providerVersion).nupkg